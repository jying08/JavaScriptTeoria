const isActive=true
const hasPermission=false

// Conversion implicita
const result=5>3
console.log(result);

const name='Platzi'
console.log(!!name);

//Conversion explicita
const value=0
const oyherValue=-7
const expliciBoolean=Boolean(value)
console.log(expliciBoolean);